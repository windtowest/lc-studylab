# LangChain 核心包 - 严格使用 1.0.3 版本（宪法级别规定）
langchain==1.0.3
langchain-core==1.0.3
langchain-openai==1.0.2
langchain-community==0.4.1

# 智谱sdk
zai-sdk==0.1.0

# transformers相关包
huggingface_hub==0.36.0
transformers==4.57.3
sentence-transformers==5.1.2


# LangGraph - 用于构建复杂工作流
langgraph==1.0.2
# langgraph-checkpoint-sqlite==3.0.0  # SQLite 检查点支持
langgraph-checkpoint-postgres==2.0.11  # PostgreSQL 检查点支持
psycopg[binary]==3.3.2  # PostgreSQL 驱动

# LangChain 集成包
langchain-tavily==0.2.13  # Tavily 搜索工具（V1.0.0 推荐）

# FastAPI 和服务器
fastapi==0.121.0
uvicorn[standard]==0.34.0

# 数据验证 - Pydantic 2
pydantic==2.12.4
pydantic-settings==2.11.0

# HTTP 客户端
httpx[socks]==0.28.1

# 环境变量管理
python-dotenv==1.0.1

# HTTP 客户端
httpx==0.28.1

# 工具相关
tavily-python==0.5.0

# 日志和监控
loguru==0.7.3

# ==================== RAG 相关依赖 ====================
# 文本分块器
langchain-text-splitters==1.0.0

# 向量数据库
faiss-cpu==1.12.0          # FAISS 向量库（CPU 版本）
# faiss-gpu==1.9.0.post1        # FAISS GPU 版本（可选，需要 CUDA）

# 文档加载器
pypdf==5.1.0                    # PDF 文档加载
unstructured==0.18.15           # 通用文档加载器
markdown==3.7                   # Markdown 支持
beautifulsoup4==4.12.3          # HTML 解析
lxml==5.3.0                     # XML 解析

# 文件处理
python-multipart==0.0.20        # 文件上传支持（FastAPI）
aiofiles==24.1.0                # 异步文件操作

# CLI 工具
click==8.1.8                    # 命令行接口框架
rich==13.9.4                    # 终端美化输出

# ==================== Self-Reward & Critic 模块依赖 ====================
# 属性测试框架
hypothesis==6.131.25            # Property-Based Testing 框架
# psycopg2-binary 已通过 psycopg[binary] 提供

# ==================== Agentic RL (Agent-Lightning + VeRL + GRPO) ====================
# Agent-Lightning - 微软 Agentic RL 框架
agentlightning>=0.1.0           # Agent-Lightning 核心包

# VeRL - 字节跳动 LLM RL 训练引擎
verl>=0.3.0                     # VeRL 核心包 (支持 GRPO, PPO 等算法)

# Ray - 分布式计算框架 (VeRL 核心依赖)
ray[default]>=2.9.0             # Ray 核心 + Dashboard

# DeepSpeed - 分布式训练优化
deepspeed>=0.14.0               # DeepSpeed ZeRO 优化

# PyTorch 相关 (VeRL 依赖)
torch>=2.1.0                    # PyTorch 核心
accelerate>=0.25.0              # HuggingFace Accelerate

# vLLM - 高效推理引擎 (VeRL 推荐)
vllm>=0.8.2                     # vLLM 推理引擎

