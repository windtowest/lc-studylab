version: '3.8'

services:
  postgres:
    image: postgres:16.9
    container_name: lc-studylab-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - lc-studylab-postgres-data:/var/lib/postgresql/data
    networks:
      - lc-studylab-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tianzuolin}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: lc-studylab-backend
    ports:
      - "8000:8000"
    environment:
      # OpenAI 配置
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_API_BASE=${OPENAI_API_BASE:-https://api.openai.com/v1}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o}
      # Tavily 搜索 API（可选）
      - TAVILY_API_KEY=${TAVILY_API_KEY:-}
      # 高德天气 API（可选）
      - AMAP_KEY=${AMAP_KEY:-}
      # 数据库配置
      - DB_URI=postgresql://${POSTGRES_USER:-tianzuolin}:${POSTGRES_PASSWORD:-TZLtzl123..}@postgres:5432/${POSTGRES_DB:-langchain}
      # 服务器配置
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8000
      - SERVER_RELOAD=false
      # 日志配置
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - LOG_FILE=logs/app.log
      # 应用配置
      - APP_NAME=LC-StudyLab
      - APP_VERSION=0.1.0
      - DEBUG=${DEBUG:-false}
    volumes:
      # 持久化数据目录
      - ./backend/data:/app/data
      - ./backend/logs:/app/logs
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - lc-studylab-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: lc-studylab-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Docker 内部使用服务名，外部访问使用 localhost
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://backend:8000}
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - lc-studylab-network

volumes:
  lc-studylab-postgres-data:
    driver: local

networks:
  lc-studylab-network:
    driver: bridge
